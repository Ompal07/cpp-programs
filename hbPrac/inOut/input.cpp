#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

int main()
{
    int a, b;
    string x;

    // cin >> a >> b >> x;

    // cout << "The inputs were: \n" << a << "\n" << b << "\n" << x;
    scanf ("Numbers: %d %d", &a, &b);

    printf ("The numbers entered were: %d %d\n", a, b);

    return 0;
}